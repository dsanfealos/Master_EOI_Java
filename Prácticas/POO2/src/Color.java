public enum Color {
    //Usamos Enum en lugar de class. La extensión del archivo es la misma.
    //Cada palabra de la lista tiene como valor su propio nombre.
    ROJO,
    VERDE,
    AZUL,
    AMARILLO,
    BLANCO,
    NEGRO,
    GRIS;  //No es necesario el ; pero se pone por costumbre, Se puede escribir tanto en fila como en columna.

}
